# NOTE: open-iris depends on onnxruntime, whose wheels only go up to
# Python 3.11. The deployment MUST run on Python 3.11 (see .python-version
# and the Streamlit Cloud "Advanced settings" Python-version selector).
streamlit
numpy
pandas
matplotlib
open-iris
fastapi
uvicorn
python-multipart
